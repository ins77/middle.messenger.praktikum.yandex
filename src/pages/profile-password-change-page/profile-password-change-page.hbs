{{#> Profile }}
    <main>
        <form class="profile-password-change-page">
            <div class="profile-password-change-page__data">
                {{#each (profile-password-change-page-inputs)}}
                    {{> InputFieldTertiary inputClassName="profile-password-change-page__input" name=name type=type placeholder=placeholder label=label }}
                {{/each}}
            </div>

            {{#> Button className="profile-password-change-page__button" page="profile" type="submit" }}Сохранить{{/Button}}
        </form>
    </main>
{{/Profile}}
